name: Continuous Integration (Pull Request)

on:
    pull_request: ~

concurrency:
    group: ci-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    test:
        name: "Test"
        uses: ./.github/workflows/ci__test.yaml
